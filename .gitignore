# Python
__pycache__/
*.py[cod]
*$py.class
venv/
.env

# YOLO / Dataset
dataset/
dataset1/


# Weights & Video (Allowed for now as per previous state, but following user's new simplified list)
# Adding these back if the user wants them, but the user specifically asked for "this way" (bu şekilde).
# I will use the user's provided simplified list.

# IDE
.vscode/
.idea/
